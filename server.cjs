'use strict';
const http=require('node:http');const fs=require('node:fs/promises');const path=require('node:path');const ROOT=path.join(__dirname,'site');const FILES=new Map([['/',['index.html','text/html; charset=utf-8']],['/index.html',['index.html','text/html; charset=utf-8']],['/about.html',['about.html','text/html; charset=utf-8']],['/assets/styles.css',['assets/styles.css','text/css; charset=utf-8']],['/assets/app.js',['assets/app.js','text/javascript; charset=utf-8']],['/assets/sentinel-mark.svg',['assets/sentinel-mark.svg','image/svg+xml']]]);const server=http.createServer(async(req,res)=>{res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('Referrer-Policy','strict-origin-when-cross-origin');if(req.method!=='GET'&&req.method!=='HEAD'){res.statusCode=405;return res.end('Method not allowed.')}let pathname;try{pathname=decodeURIComponent(new URL(req.url,'http://localhost').pathname)}catch{res.statusCode=400;return res.end('Invalid request path.')}if(pathname==='/healthz'){res.setHeader('Content-Type','application/json');return res.end(JSON.stringify({status:'ok',service:'arqs-web'}))}const file=FILES.get(pathname);if(!file){res.statusCode=404;return res.end('Page not found.')}try{const content=await fs.readFile(path.join(ROOT,file[0]));res.setHeader('Content-Type',file[1]);res.statusCode=200;return res.end(req.method==='HEAD'?undefined:content)}catch(error){console.error('ARQS public file unavailable:',file[0],error.code);res.statusCode=500;return res.end('The website is temporarily unavailable.')}});const port=Number(process.env.PORT||3000);if(!Number.isInteger(port)||port<1||port>65535){console.error('PORT must be an integer between 1 and 65535.');process.exit(1)}server.on('error',error=>{console.error('ARQS server could not start:',error.code);process.exit(1)});server.listen(port,'0.0.0.0',()=>console.log(`ARQS web server listening on port ${port}`));
